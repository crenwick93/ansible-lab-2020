---
- name: install mariadb database
  hosts: db
  become: true
  tasks:
    - name: install mariadb-server & python3-PyMySQL
      yum:


    - name: start mariadb
      service:


    - name: mysql_root_password
      mysql_user:
